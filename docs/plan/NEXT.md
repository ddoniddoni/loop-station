# 다음 개발 작업

갱신일: 2026-09-27. 이 문서는 실행 순서를 관리한다. 실제 구현·검증 이력은 [진행 기록](../PROGRESS.md), 전체 범위는 [로드맵](README.md)을 따른다.

## 현재 위치

- Phase 0·1·2: 부분 구현·미검증. Tap Tempo와 녹음 길이·v5 작업은 develop에 통합했다. 입력 채널 선택을 구현했고 다음 기능은 AUTO 모니터링이다.
- 현재 작업 브랜치: 원격 develop `e569efc`에서 시작한 `feature/phase-1-input-channels`. 키보드·카운트인 브랜치는 보존했고 이번 작업에 병합하지 않았다.
- 통합 상태: 사용자 요청으로 `feature/phase-1-tap-tempo`의 `8c4ed6f`를 develop에 병합했다. 다음 개발은 최신 원격 develop에서 새 작업 브랜치를 만든 뒤 시작한다.
- 카운트인 ON/OFF는 `feature/phase-1-count-in`의 `f7195e0`에 구현·푸시되어 있으나 develop에는 아직 통합하지 않았다. merge는 별도 사용자 요청 범위다.
- 녹음 길이 통합: 최신 develop `5478f3d`에서 만든 `feature/phase-2-recording-length`의 `97f7eff`를 커밋·푸시하고 develop에 병합했다. 트랙별 1/2/4/8마디 선택, 길이에 맞는 녹음·오버더빙·복구와 v5 저장을 포함한다. 기능 테스트는 미실행이다.
- 이전부터 구현한 기반: 8트랙 PCM, 공통 시계, Undo/Redo·Clear 복구, 로컬 자동 저장, 볼륨·팬·Mute·Solo, 루프 마스터·PCM 미터.
- 저장 계약: 1/2/4/8마디 선택과 v5 저장, v1/v2/v3/v4 읽기 이전을 사용한다. 기본 녹음 길이는 4마디이며 입력 모니터링은 기본 OFF다.

## 이전 작업 — Phase 0 오디오 시작·종료 안정화

**상태: 구현 완료/미검증.** 요구사항 `SYS-03`, 관련 `SYS-04`와 Phase 0 통과 기준. 이번 안정화 범위의 코드와 회귀 시나리오를 작성했고 린트·타입 검사·빌드·React Doctor를 확인했다. 실제 오디오·단위·E2E 검증은 사용자 중단 요청에 따라 보류한다.

- 실제 Worklet의 첫 오디오 블록 처리 응답을 받은 뒤 준비 완료로 전환한다. 처리 응답의 버전·샘플레이트·블록 길이를 확인한다.
- 파일 로딩/오디오 준비의 무응답을 구분해 안내하고 사용자가 취소·재시도할 수 있게 한다. 시간 제한은 시작 실패 감지에만 사용하며 루프 시계로 사용하지 않는다.
- 중복 시작·초기화·종료를 방지한다. 시작 취소·오류 정리·재시작이 겹쳐 이전 작업이 새 상태나 마이크/트랙을 정리하지 않도록 한다.
- 사용자 동작 전 오디오/마이크 생성 없음, 모니터링 OFF, 마이크 없이 테스트 신호 사용이라는 기존 경계를 유지한다.
- 수명·오류 경로의 회귀 테스트와 production Worklet 시작/종료 E2E를 작성한다. 기존 테스트 중단 요청은 유지하며 미실행을 통과로 표시하지 않는다.

수정 전 코드 점검 근거: `initialize()`는 노드 생성 후 첫 처리 응답 없이 끝나며 `isReady`는 노드 존재만 검사했다. `stopAudio()`는 `close()` 완료 전에 엔진 참조를 비우고, 초기화 실패 정리 후에는 이전 작업인지 다시 확인하지 않았다. 첫 처리 응답, 시작 15초 제한, 동일 초기화/종료 Promise 공유와 작업 번호 확인, 종료 실패 재시도를 추가했다. 실제 브라우저 재현은 미실행이다.

Phase 0 종료 판단에는 실제 production Worklet 로딩·처리, 중복 AudioContext 방지, dispose, 권한 거부·입력 해제 검증이 필요하다. 이번 구현만으로 Phase 0 전체를 완료로 바꾸지 않는다.

### 남은 Phase 0 통과 항목

- [x] 실제 처리 블록의 준비 응답, 응답 정보 검사와 무응답 안내를 구현한다.
- [x] 시작·취소·종료의 수명 처리를 보강하고 종료 실패 재시도를 제공한다.
- [x] `tests/audio/audio-engine.test.ts`의 단위 11개와 `tests/e2e/audio-lifecycle.spec.ts`의 production 오디오 시나리오 4개를 작성한다. 작성은 통과 증거가 아니다.
- [x] 허용된 정적 검사·빌드 결과와 미검증 범위를 `PROGRESS.md`에 기록한다.
- [ ] 테스트 재개 요청 후 단위 테스트와 production E2E를 실행하고 발생한 오류를 수정한다. 새 오디오 E2E는 마이크 없이 실제 Worklet 준비·테스트 신호 PCM·취소 후 지연 응답·재시작·실패 재시도를 다룬다.
- [ ] 권한 허용/거부·장치 없음·늦은 권한 응답 취소·장치 전환/해제, 기본 OFF 모니터링과 환경 진단을 브라우저에서 확인한다.
- [ ] 개발 모드의 Strict Mode·Fast Refresh와 production의 중복 Context·dispose, 실제 출력 장치 청취를 확인한다. 합성 PCM 검사는 장치 청취를 대신하지 않는다.

Phase 0 통과 항목은 테스트 재개 후 먼저 확인한다. 최신 사용자 요청에 따라 후속 기능을 개발하되 테스트 중단 요청은 유지한다.

## 직전 구현 — Tap Tempo

**상태: 구현 완료/미검증, develop 통합.** Phase 1 / 요구사항 `CLK-01`, 상세 명세 5.4절의 4분음표 BPM 계약. Tap Tempo의 develop 통합은 완료했고 카운트인 코드는 별도 브랜치에 보존한다.

### 사용자 결과와 정책

- 상단 BPM 설정의 `TAP · 템포 맞추기`를 두 번 이상 누르면 BPM 입력칸에 제안값이 나온다. `설정 적용`을 눌러 기존 엔진 명령 경로로 반영한다. 준비되지 않은 오디오를 자동 시작하거나 마이크를 요청하지 않는다.
- 최근 최대 4개 유효 간격의 평균으로 정수 BPM을 계산한다. 범위는 40~240, 6/8·7/8도 4분음표 기준이다. 클릭 이벤트의 입력 시각은 제안값 계산에만 쓰고 Worklet 공통 시계를 대체하지 않는다.
- 250ms 미만은 마지막 유효 시각을 움직이지 않고 제외한다. 1,500ms 초과이면 이전 간격을 비우고 해당 Tap부터 다시 센다. 유효하지 않거나 역행한 시각은 제외한다. Enter/Space를 계속 누를 때 반복 입력을 막는다.
- 수동 BPM 입력·설정 적용·설정창 닫기·오디오 준비/잠금 상태 변경 때 Tap 기록을 초기화한다. 설정창을 다시 열면 현재 적용 BPM·박자를 표시한다. 적용 전 초안은 저장하지 않는다.
- 기존 루프·비운 루프의 복구 이력·녹음 준비·편집·저장소 잠금과 오디오 준비 여부를 그대로 따른다. 설정 적용 시 UI와 기존 엔진 모두 잠금을 확인한다. 카운트인 통합 후에는 동일한 프로젝트 잠금 경로를 사용하며 통합 검증이 필요하다.

### 구현·검증 체크리스트

- [x] 관련 명세, 템포 UI·공통 시계·엔진과 프로젝트 잠금 경로를 확인한다.
- [x] React/브라우저에 의존하지 않는 제한된 Tap 간격 계산기를 구현한다.
- [x] Radix 버튼·BPM 제안값·안내·설정 적용·초기화·잠금을 연결한다.
- [x] 단위 18개와 production E2E 시나리오 4개를 작성한다. 작성은 통과 증거가 아니며 모두 미실행이다.
- [x] 린트·타입 검사·빌드·React Doctor 결과와 미검증 범위를 `PROGRESS.md`에 기록한다.
- [ ] 테스트 재개 후 계산 경계값·초기화·실제 Worklet 설정 반영·잠금과 모바일/키보드 조작을 확인한다.

이번 범위는 Tap으로 BPM을 제안하고 기존 설정 경로로 적용하는 기능이다. 빈 프로젝트 템포의 독립 저장, 글로벌 Tap 단축키, 재생 중 루프의 템포 변환, 첫 자유 루프로 템포 설정, 키보드 연주 전체는 후속 범위다. 실제 검증 전에는 Phase 1이나 `CLK-01` 전체를 검증 완료로 변경하지 않는다.

## 별도 브랜치 — 녹음 전 1마디 카운트인

`feature/phase-1-count-in` / `f7195e0`: 기본 ON의 1마디 카운트인, Worklet 절대 프레임 예약·클릭 출력·남은 박 표시·취소·입력 끊김 처리를 구현했다. 4마디·v4 저장 기준이며 설정은 탭에서만 유지하고 새로고침 시 ON이다. 단위 26개와 E2E 시나리오 2개는 미실행이다. 실제 범위와 검사 기록은 해당 브랜치의 `PROGRESS.md`를 따른다. 아직 develop과 이번 브랜치에 통합하지 않았으며, Tap Tempo와 함께 통합한 뒤 준비 중 템포 잠금 등을 검증해야 한다.

## 별도 브랜치 — 기본 키보드 연주 조작

**상태: 구현 완료/미검증, develop 미통합.** Phase 2 / 요구사항 `MIDI-01`, 상세 명세 4.6절. `feature/phase-2-keyboard-controls` / `40dafae`에 구현·푸시했다. 이번 브랜치의 제품 코드에는 포함되지 않는다.

- 기본 OFF의 키보드 연주, Space 재생/정지, 1~8 트랙 선택, R 녹음/오버더빙, S 정지, Esc 취소, Cmd/Ctrl+Z 및 Shift 조합 Undo/Redo를 기존 명령에 연결했다.
- 입력 필드·Radix 조작·열린 오버레이·IME·반복 입력·기존 잠금을 따른다. 단위 20개·E2E 5개는 작성만 했으며 해당 브랜치의 진행 기록을 따른다.

## 이번 구현 — 입력 채널 선택

**상태: 구현 완료/미검증.** Phase 1 / `IN-01`, 관련 `IN-02`. IN-01 전체와 Phase 1의 인수 기준 통과를 뜻하지 않는다.

- 모노 합산(기본), 왼쪽/CH1, 오른쪽/CH2를 선택한다. 실제 `getSettings().channelCount`가 알려 주는 범위만 허용하고 확인 불가이면 합산만 허용한다. 스테레오 녹음은 이유와 함께 비활성화한다.
- 입력 게인 앞의 라우팅을 선택해 녹음·미터·모니터가 같은 모노 신호를 사용한다. 녹음 준비·대기·녹음·오버더빙 중 변경과 연결 재시도를 막는다. 장치 전환 중에도 변경하지 않는다.
- 채널 변경 시 모니터 OFF, 미터·클리핑 초기화와 revision 갱신으로 이전 메시지를 무시한다. 성공한 장치 연결·입력 해제·오디오 재시작은 기본 합산으로 돌아간다. 실패/취소된 장치 전환은 기존 경로를 유지한다. 라우팅 실패는 입력을 끊고 재시도로 복구한다.
- 설정은 탭에서만 유지한다. PCM 포맷·공통 시계·v5 저장은 변경하지 않는다. 현재는 개별 CH1/CH2만 선택하며 다채널 전체 선택·스테레오 녹음·동적 장치 채널 수 변화 검증은 남는다.
- 단위 시나리오와 합성 스테레오 PCM → 실제 입력 버스 → production Worklet의 녹음·입력 미터·모니터 출력 E2E를 작성했다. 모두 미실행이며 실제 장치/브라우저·채널 간 분리·실청취·UI 조작은 미검증이다. 허용된 검사 결과는 `PROGRESS.md`를 따른다.

## 다음 작업 — AUTO 모니터링

**상태: 미착수.** Phase 1 / `IN-02`, 상세 명세 5.2절 입력 라우팅 계약.

- 기존 OFF/ON에 AUTO를 추가한다. 기본은 OFF이며 사용자 선택 없이 마이크나 모니터를 활성화하지 않는다.
- AUTO는 해당 입력을 사용하는 트랙이 실제 녹음/오버더빙 중일 때만 한 번 출력한다. 준비·예약 대기·카운트인·재생만 하는 구간은 제외한다. 오디오 프레임의 캡처 상태를 기준으로 하고 React 렌더링 지연이 경계가 되지 않게 설계한다.
- 장치/채널 변경·입력 해제·오디오 중단에는 안전하게 OFF로 돌아간다. 듣기 음량은 녹음·입력 미터와 분리한다. 음성 보정 옵션과 스테레오 녹음은 후속 범위다.
- 수용 기준: 실제 녹음 시작/끝과 취소·중단에서 모니터 PCM이 정확히 열리고 닫히며, 입력과 녹음 PCM은 모니터 모드에 영향을 받지 않는다. 합성 PCM·단위·UI 시나리오를 작성하고 테스트 중단 요청은 재개 지시 전까지 유지한다.
- 착수 시 최신 develop과 미통합 브랜치를 확인한다. 채널 선택·키보드·카운트인 통합이 필요하면 기존 merge 승인 범위를 확인하고, 미통합 코드를 완료된 기반으로 가정하지 않는다.

## 후속 우선순위

아래는 작업 선택 순서이며, 한 항목이 크면 다시 작은 기능 단위로 나눈다. 후속 항목은 착수할 때 세부 범위와 인수 기준을 확정한다.

| 순서 | 작업 묶음 | 연결 Phase |
|---|---|---|
| 재개 시 우선 | Phase 0 통과 항목과 Tap Tempo·카운트인 등 누적 검증 | 0·1·2 |
| 1 | AUTO 모니터링 | 1 |
| 2 | 음성 보정 옵션·추가 실행 예약 등 남은 기본 조작 | 1 |
| 3 | 프로젝트 목록·새 프로젝트·데모와 첫 사용 안내 | 2 |
| 4 | 첫 자유 루프의 템포 설정, 오버더빙 세션·이력 등 Phase 2 미충족 항목 정리 | 2 |
| 5 | 지연 보정·편집·WAV·ZIP의 작은 기능 단위 | 3 |

검증 재개 지시가 오면 순서와 관계없이 누적된 PCM·Worklet·저장 복구·장시간 동기 검증을 우선한다. Phase 3 본격 확장 전에 Phase 0~2의 치명적 오류와 미충족 인수 기준을 확인한다. 이후 Phase 4~8은 로드맵 순서로 진행하며 클라우드는 사용자 선택 범위로 유지한다.

## 현재 검증 방침

- 사용자 요청으로 단위·오디오·E2E·브라우저·실청취 테스트는 중단 상태다. 이 계획을 정리한 요청은 테스트 재개 지시가 아니다.
- 린트·타입 검사·빌드 및 필요한 정적 검사는 실행한다. 테스트 코드를 작성할 수 있지만 실행 여부를 구분한다.
- 재개 시 오디오 수명·PCM·오버더빙·이력·저장 단위와 `tap-tempo.test.ts`, `tap-tempo.spec.ts`, `local-save.spec.ts`, `mixer.spec.ts`, `stereo-output.spec.ts`를 확인한다. 길이별 PCM·`mixed-loop-lengths.test.ts`와 v1~v4→v5 저장 이전도 확인한다. 카운트인 검증은 별도 카운트인 브랜치에 있으므로 해당 통합 시 함께 확인한다.
- 실제 장치 지연·청취·브라우저별 장시간 연주는 자동 검증과 별도 기록한다.
- 입력 채널의 `input-channels.test.ts`와 `input-channels.spec.ts`도 재개 후 실행한다. 모노/스테레오 실장치와 채널 수 미보고 장치, 변경 잠금·재연결·미터·모니터 초기화는 별도 브라우저/장치 검증이 필요하다.
